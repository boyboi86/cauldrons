/**
 * Inlined at build time by Vite `define` (see vite.config.ts) as a four-digit
 * year string, e.g. "2026". Because both the prerender build and the client
 * build receive the same literal, the first client render matches the
 * prerendered markup and hydration stays silent.
 */
declare const __BUILD_YEAR__: string
